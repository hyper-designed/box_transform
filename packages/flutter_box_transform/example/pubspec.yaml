name: example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  adaptive_theme: ^3.2.1
  url_launcher: ^6.1.11
  provider: ^6.0.5
  vector_math: ^2.1.4
  flutter_box_transform:
    path: ../
  box_transform:
    path: ../../box_transform

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true

